#!/usr/bin/env bash

set -e

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source $DIR/base
cd "$DIR/../docker/minio"

docker build -t gcr.io/$PROJECT_NAME/siacdn-minio:latest .
gcloud docker -- push gcr.io/$PROJECT_NAME/siacdn-minio:latest
