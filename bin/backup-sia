#!/usr/bin/env bash

set -e

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source $DIR/base

PODNAME=$($KUBE get pods -l app=sia -o jsonpath="{.items[0].metadata.name}")

$KUBE exec -it $PODNAME -c sia-docker-backup-periodic sh /root/backup.sh
