#!/usr/bin/env bash

set -e

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source $DIR/base
cd "$DIR/../www"

docker build -t gcr.io/$PROJECT_NAME/siacdn-www:latest .
gcloud docker -- push gcr.io/$PROJECT_NAME/siacdn-www:latest
