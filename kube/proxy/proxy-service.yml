apiVersion: v1
kind: Service
metadata:
  name: proxy
  namespace: sia
spec:
  type: NodePort
  ports:
    - name: http
      port: 9096
      targetPort: 9096
      protocol: TCP
  selector:
    app: proxy
