#!/usr/bin/env bash

set -e

PROJECT_NAME="gradientzoo-1233"
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

cd "$DIR/../../backend"

docker build -t gcr.io/$PROJECT_NAME/siacdn-backend:latest .

gcloud docker -- push gcr.io/$PROJECT_NAME/siacdn-backend:latest
