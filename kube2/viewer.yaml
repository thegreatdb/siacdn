apiVersion: v1
kind: Service
metadata:
  name: siacdn-viewer
spec:
  clusterIP: None
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: siacdn-viewer
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: siacdn-viewer
spec:
  selector:
    matchLabels:
      app: siacdn-viewer
  serviceName: "siacdn-viewer"
  replicas: 3
  template:
    metadata:
      labels:
        app: siacdn-viewer
    spec:
      terminationGracePeriodSeconds: 10
      containers:
      - name: sia
        image: ericflo/siacdn-viewnode:latest
        imagePullPolicy: Always
        command: ["siad-wrapper.sh", "-d", "/etc/sia"]
        ports:
        - containerPort: 9980
        volumeMounts:
        - name: sia-viewer
          mountPath: /etc/sia
        - name: sia-viewer-renter-fs
          mountPath: /etc/sia/renter/fs
        envFrom:
        - secretRef:
            name: siacdn-viewer
        readinessProbe:
          exec:
            command:
            - /go/bin/readiness.sh
          initialDelaySeconds: 30
          periodSeconds: 60
          timeoutSeconds: 5
        resources:
          requests:
            memory: "2Gi"
            cpu: "250m"
          limits:
            memory: "8Gi"
            cpu: "6500m"
      - name: nginx
        image: ericflo/siacdn-nginx:latest
        imagePullPolicy: Always
        command: ["/etc/skynet/start-viewer.sh"]
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: sia-viewer
          mountPath: /etc/sia
        envFrom:
        - configMapRef:
            name: siacdn-config
        resources:
          requests:
            memory: "256Mi"
            cpu: "125m"
          limits:
            memory: "500Mi"
            cpu: "500m"
  volumeClaimTemplates:
  - metadata:
      name: sia-viewer
    spec:
      storageClassName: "microk8s-hostpath"
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 50Gi
  - metadata:
      name: sia-viewer-renter-fs
    spec:
      storageClassName: "rook-ceph-block"
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 50Gi